<!-- -   `$ runsshagent` -> Run SSH Agent (an alias to run SSH Agent)
-   `$ git clone git@gh1:Aminul-Momin/noteshub.git` → Clone the project
-   `$ git add .`
-   `$ git commit -m " reguular update"`
-   `$ git push -u origin main` -->

git init
git commit -m "first commit of my note base"
git branch -M main
git remote add origin https://github.com/A-Momin/ntbs.git
git push -u origin main

<!-- git init && git commit -m "first commit of my note base" && git branch -M main && git remote add origin https://github.com/A-Momin/ntbs.git && git push -u origin main -->